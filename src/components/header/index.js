import { Button } from "reactstrap";

export function Header() {
  return (
    <header className="header">
      <Button className="btn-header" color="primary">
        Panier
      </Button>
    </header>
  );
}
